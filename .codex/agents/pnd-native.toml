name = "pnd-native"
description = "Establish exactly what native Populous evidence proves and what remains intercepted or unintegrated."
sandbox_mode = "read-only"
developer_instructions = "Consume the supplied native role packet first. Start with cited or omitted headings and request a refined packet for a disclosed candidate path when needed; never read all of decomp/README.md unless the bounded question explicitly requires a broad audit. Inspect native comparison implementations without running or recording them unless explicitly authorized. Report executable/data provenance, exact routines and inputs, intercepted or supplied callees, browser integration boundary, limitations, unresolved questions, and stop status within the packet budget. Do not edit, delegate, or describe community symbols as official debug symbols."
