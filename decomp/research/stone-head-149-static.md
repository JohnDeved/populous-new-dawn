# Static Totem/model149 prerequisite — Worker1, #21

Scope: base149 asset and source-qualified campaign presentation only, based on
main `4897ea93833b5062aabc357a9aa54787f150c44e`. This does **not** complete #21 or
claim native animation cadence. Historical research is retained verbatim in
`stone-head-149.md` (ac0e9852) and `worship-totem-model.md` (c7bb3d76); their old
ownership/status statements describe those earlier handoffs, not this continuation.

## Source and reuse

The ac0e985 28-condition receipt is reused, not rerun against a new native timing
harness. Preserved receipt SHA-256:
`e48cce7fa0d8b2a6117fe4c592a772614a9fa85ffd0b7ef0e9ecc93d092e7f74`.
Its original location is the preserved `issue-21-stone-head-149` worktree at
`work/orchestration/issue21-149/asset-proof.json`. This continuation records the
verified checksum, raw input hashes and count in
`work/orchestration/issue21-static149/reuse.json`.

`004851e0` links an authored class6/model6 trigger to the first class5/model9
scenery in the same native512-unit cell. Linked class6/model2 rewards derive the
0x10/0x20 appearance flags. The retained `004fbd20` selector evidence identifies
mode0 with neither flag as base149, not a spell head45. Native durations9+9 in
morph record7 are animation visits only; their wall-time cadence remains unresolved.

The new selector deliberately proves only mode0/flags0. It matches authored
position/range/trigger angle, rejects ambiguous or incomplete sources, verifies all
links and uses the existing scenery-cell helper. It does not classify a head by
name, reward kind, a legacy model45 fallback, or missing animation state alone.
Mission10 root119/scenery118 and linked58/scenery59 resolve149, with original
scenery headings0 and1024. Mission3 trigger101 and Mission5 triggers96/101/113 are
additional retained positive controls; spell45, Obelisk8, Angel157 and Vault154
remain separate.

## Asset and generated-output ownership

`scripts/import-stone-head-149.py GAME_ROOT --write` owns exactly:

- `app/original-models.json`: append key149, preserving all77 prior models and their
  bytes. No key150/151 or other runtime slot is added.
- `public/original/provenance.json`: append modelIds149 and staticStoneHead149,
  retaining every existing provenance value.

Without `--write`, the same command is a read-only source/import verification.
It reuses `decode_original_model` from `scripts/import-original.py`; no duplicate
mesh decoder, executable run or full-asset regeneration is introduced. It pins all
three bank2 input hashes, the exact base object record and corresponding point
indices, and the canonical decoded SHA-256:
`4500fc0e919cc8362f22900a100918f7aceb4c779212a09f2fb9c6724ee19a53`.
The asset has51 faces,51 raw points,258 expanded vertices/86 triangles and scale160.
Original faces, UVs, modes, normals and painter biases are retained.

Only tiles159 and167 are used; both are covered by the existing256x1024 atlas.
Atlas SHA-256 remains
`fdb5c2af7ca43debb5d943036969df29949773b6b94c0b7ce99ffc5d946b27b0`.
The continuation's `asset-ownership.json` records byte-idempotence, preservation of
all existing models/provenance/PNGs, and refusal of differing model data, incorrect
source hashes and an unreviewed atlas. These are supplemental static checks, not
new native cadence evidence.

## Runtime boundary and verification

`stoneHead149Model` is called only at the two ordinary shrine mesh creation/
replacement sites in `scene-entities.ts`. Controller model45, work/reward state,
existing visibility/retirement behavior, headings and checkpoint schema remain
unchanged. Both latent linked and restored authored heads derive the static asset.
Existing live morphs/stone-head families are protected. No149 animation state,
key150/151 runtime assets, audio, worship panels, simulation or clock changes exist.
The touched scene is formatted; its pre-existing visibility condition is named
`activeHead` to retain the unchanged whitespace-sensitive legacy assertion.

Focused command:
`node --test tests/stone-head-149.test.mjs tests/stone-head-animation.test.mjs tests/stone-head-orientation.test.mjs`.
The authored browser checker is `scripts/check-browser-stone-head-149.mjs`, run
through the shared queue's `supervise` mode. It enters Mission10 through real UI,
checks actual static mesh buffers, GPU pixels, scenery headings and pointer hits,
orders authored followers through the HUD to worship normally, observes linked
Totem creation, and checks the ordinary checkpoint UI. Only camera focus,
test-owned RAF and normal tick acceleration are supplied; no actors, work or
rewards are injected. This is not animation-cadence or FPS acceptance.

Run-specific results and README TypeScript quality findings belong to the ignored
continuation packet and immutable shared queue receipts. Keep failures/advisory
findings distinct from passing required gates. Independent integration review is
still required; do not mark gameplay parity or close #21 from this static slice.
